# Local preview without paid API calls. Set false for real responses.
AI_DEMO_MODE=true
# Server-only. Never prefix secrets with NEXT_PUBLIC_.
OPENAI_API_KEY=
# Set a Responses API model available to your account when disabling demo mode.
OPENAI_MODEL=
AI_MAX_OUTPUT_TOKENS=1024
